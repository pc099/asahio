# Core
fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1
pydantic>=2.0.0,<3
pydantic-settings>=2.0.0,<3

# Database
sqlalchemy[asyncio]>=2.0.0,<3
asyncpg>=0.30.0,<1
alembic>=1.14.0,<2
aiosqlite>=0.20.0,<1

# Redis
redis[hiredis]>=5.0.0,<6

# Stripe
stripe>=11.0.0,<12

# YAML config
PyYAML>=6.0.0,<7

# HTTP client
httpx>=0.27.0,<1

# Auth (Clerk JWT verification)
PyJWT[crypto]>=2.8.0,<3
cryptography>=43.0.0

# Background tasks
celery[redis]>=5.4.0,<6

# Email
resend>=2.0.0,<3

# LLM providers (for gateway bridge to existing src/)
openai>=1.0.0,<2
anthropic>=0.39.0,<1

# Embeddings (local model for semantic cache)
sentence-transformers>=3.0.0,<4

# Vector store (semantic cache)
pinecone>=5.0.0,<7

# Cohere embeddings (production)
cohere>=5.0.0,<6

# Dev
pytest>=8.0.0,<9
pytest-asyncio>=0.24.0,<1
httpx  # for TestClient
black>=24.0.0
ruff>=0.8.0
